package rules;

import com.frauddetection.domain.KYC;

rule "Unverified KYC"
when
    $k: KYC( verifiedStatus == false )
then
    $k.addViolation("KYC_UNVERIFIED", 15, "User is not KYC verified");
end

