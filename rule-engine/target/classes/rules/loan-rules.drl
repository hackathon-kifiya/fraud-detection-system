package rules;

import com.frauddetection.domain.DynamicFact;

rule "High loan amount"
when
    $l: DynamicFact( dataType == "loan", getDoubleProperty("amountRequested") > 50000 )
then
    $l.addViolation("LOAN_HIGH_AMOUNT", 20, "Loan amount exceeds threshold");
end

