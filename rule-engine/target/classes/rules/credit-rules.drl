package rules;

import com.frauddetection.domain.CreditHistory;

rule "Low credit score"
when
    $c: CreditHistory( creditScore < 500 )
then
    $c.addViolation("CREDIT_LOW_SCORE", 20, "Low credit score");
end

