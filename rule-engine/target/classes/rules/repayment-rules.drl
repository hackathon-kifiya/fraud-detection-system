package rules;

import com.frauddetection.domain.DynamicFact;

rule "Late repayment"
when
    $r: DynamicFact( dataType == "repayment", getStringProperty("status") == "late" )
then
    $r.addViolation("REPAY_LATE", 10, "Repayment marked late");
end

